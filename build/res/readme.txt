copy these files into your build folder near simple executable.

Simple has been tested on chromium and it does not work due to the fact that chromium can't load the assets file required by Simple.
But you can run Simple using Firefox!
